export default (state = [], action) => {
  switch ((action.type)) {
    default:
      return state;
  }
};
// the action is an object
// the state must have a default value of whatever it returns
// when the app launches it return the default value of all reducers.
